

// tres test 3T
let estatus = ['testing','TODO','done','urgente'];

let tiempo = new Date();
tiempo.date = tiempo.toLocaleDateString();
tiempo.time = tiempo.toLocaleTimeString();

let t = 'mejorar testing demomento solo para index y github';

document.title = estatus[0];
console.log(t, tiempo.time, tiempo.date);
//prompt(t);

// hacer bit_core a la par. enlazar localStorage para crear archivos de texto, descargarlos (generar archivo);
// buscar limite de memoria, demomento limite = qr


// mejorar testing demomento solo para index y github


